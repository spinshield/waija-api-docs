Object.assign(window.DOCS_DEFAULT.pages,{"credit-and-debit":{"title":"Callback Action: debit / credit","crumb":"Callbacks","lede":"Debit and credit callbacks update a player's balance during real-money play.","body":"A `debit` removes `amount` from the player wallet. A `credit` adds `amount` back. If a debit cannot be applied because of **insufficient balance**, return `error: 1` together with the current `balance`.\n\n`call_id` is a unique transaction identifier.\n\n!!! Important\nAlways answer with HTTP `200`. Success and failure are communicated **only via the JSON response body**.\n!!!\n\n## Key Behavior Rules\n\n- Process `debit` and `credit` callbacks **independently**\n- A `credit` is not always paired 1:1 with the most recent `debit`\n- Match players using `username`, and keep `game_id`, `currency`, `round_id` and `gameplay_final` for reconciliation\n\n## Rollbacks\n\nRollback callbacks include `rb: 1`. They reverse a previous `debit` or `credit` for the same `round_id`.\n\nProcess rollback callbacks the same way as regular debit/credit actions.\n\n## Requests\n\nCallbacks arrive as `GET` requests.\n\n### Spin Callback\n\n```json\n{\n  \"action\": \"debit\",\n  \"amount\": 100,\n  \"username\": \"yourPlayerId9959\",\n  \"currency\": \"USD\",\n  \"game_id\": \"onlyplay/SaintBananas\",\n  \"round_id\": \"3719823\",\n  \"call_id\": \"8b54edf4f1d4c3efdafd90e5fd92c99d\",\n  \"gameplay_final\": 0,\n  \"rb\": 0\n}\n```\n\n### Rollback Spin Callback\n\n```json\n{\n  \"action\": \"credit\",\n  \"amount\": 100,\n  \"username\": \"yourPlayerId9959\",\n  \"currency\": \"USD\",\n  \"game_id\": \"onlyplay/SaintBananas\",\n  \"round_id\": \"3719823\",\n  \"call_id\": \"rollback-8b54edf4f1d4c3ef\",\n  \"gameplay_final\": 0,\n  \"rb\": 1\n}\n```\n\n### Free Round Spin Callback\n\nFree spins granted through [addFreeRounds](#add-freerounds) should **not deduct the player's balance**. Identify them by `type: bonus_fs`.\n\n```json\n{\n  \"action\": \"debit\",\n  \"amount\": 0,\n  \"type\": \"bonus_fs\",\n  \"username\": \"yourPlayerId9959\",\n  \"currency\": \"USD\",\n  \"game_id\": \"softswiss/LuckyCrew\",\n  \"round_id\": \"24\",\n  \"call_id\": \"fs-8b54edf4f1d4c3ef\",\n  \"gameplay_final\": 0,\n  \"rb\": 0\n}\n```\n\n## Responses\n\nBalance is an **integer in cents**. `$2.50` → `250`. Always HTTP `200`.\n\n### Successful Response\n\n```json\n{\n  \"error\": 0,\n  \"balance\": 250\n}\n```\n\n### Insufficient Balance\n\n```json\n{\n  \"error\": 1,\n  \"balance\": 250\n}\n```\n\n### Processing Error\n\nReturned when the player is not found or the signature is invalid.\n\n```json\n{\n  \"error\": 2,\n  \"balance\": 0\n}\n```\n"},"validation":{"title":"Callback Validation","crumb":"Callbacks","lede":"Every callback includes a `key` and a `timestamp` so you can verify the authenticity and freshness of the request.","body":"Validation ensures that the callback:\n\n- Originates from Slotsgateway\n- Has not been replayed or delayed\n\n!!! Security Requirement\nThe `timestamp` **must be within the last 30 seconds**. If it is older, you **must reject the callback** with error `2` and HTTP `200`.\n!!!\n\n## Signature Generation\n\nThe `key` is an MD5 hash of `timestamp + salt key`. The salt key is unique per API key and is available in the [Backoffice](https://backoffice.waija.com).\n\n### Key Construction (JavaScript)\n\n```js\nmd5(timestamp + saltkey);\n```\n\n### Key Construction (PHP)\n\n```php\nmd5($timestamp . $saltkey);\n```\n\n## Validation Process\n\n1. Read `timestamp` and `key` from the callback\n2. Confirm the timestamp is no older than 30 seconds\n3. Recreate the signature using the same timestamp and your salt key\n4. Compare it to the received `key`\n5. If validation fails, reject the callback\n\n## Invalid Signature / Timestamp Response\n\nEven on errors, return HTTP `200`.\n\n```json\n{\n  \"error\": 2,\n  \"balance\": 0\n}\n```\n"},"php-client":{"title":"PHP Client","crumb":"Tools","lede":"If your project uses PHP, we provide a Composer package to speed up integration.","body":"- GitHub package: [slotsgateway/slotsgateway-php-client](https://github.com/slotsgateway/slotsgateway-php-client)\n- Install via Composer:\n\n```bash\ncomposer require slotsgateway/slotsgateway-php-client\n```\n\nUse the client against the API Base URL from **Settings**, with the `api_login` and `api_password` generated in the backoffice.\n"},"postman":{"title":"Postman","crumb":"Tools","lede":"You can use our public Postman collection to perform automated testing and assist integrating.","body":"Load the collection within Postman, then replace the sample `api_login`, `api_password` and API Base URL with the values from your backoffice Settings.\n\n[View Postman Collection](https://github.com/games2api/public-share/blob/main/waija_postman_testtool.json)\n"}});